# AGENTS.md

## Asset Guidelines

- **Textures**: Use WebP where supported, capped at the smallest practical dimensions (256×256 for small props). Use JPG at quality 75–85 when WebP is unsuitable. Avoid PNG for photographic/image textures — PNG should only be used for sprites, UI elements, or images requiring transparency.
